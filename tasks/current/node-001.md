# Node 001: {node title}

> 範本來源 `ai-workflow/node-template.md`。複製本檔為 node-002、node-003…。
> 執行鐵則：只改 Allowed Files；碰 Forbidden Changes / scope 不足 / 文件衝突 → 停下回報。

## Goal
本節點要完成什麼。一句話講清楚可驗證的結果。

## Background
為什麼需要這個節點。它在 feature 裡的位置。

## Scope
允許處理的範圍。

## Out of Scope
本節點明確不處理的範圍。

## Inputs
需要讀取的文件、設計、issue、程式碼（列出具體路徑）。
> 遵守 `ai-workflow/context-policy.md` 最小上下文白名單。

## Allowed Files
允許新增 / 修改的檔案或資料夾（盡量精確到目錄或檔名）。

## Forbidden Changes
明確禁止修改的範圍。

## Tasks
1. 任務一
2. 任務二

## Quality Gates
1. 架構符合 docs/architecture.md
2. 命名符合既有慣例
3. 無新增未紀錄的相依套件
4. 無跨模組直接依賴內部實作
5. 無未處理的錯誤狀態

## Tests
需要執行的測試與通過標準。

## Review Checklist
引用 `ai-workflow/review-checklist.md`，標出本 Node 特別關注的項目。

## Output
完成後要更新哪些文件 / 產出哪些報告。

## Handoff
下一個 Node 或下一個 Skill。
